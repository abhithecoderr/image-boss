Manages background tasks that occur when control values change, without having to press process button.

Currently handles these tasks:

1. Estimate file size calculate when compression quality value changes

2. resultCanvas adjusted in bg removal when slider values adjusted, for post processing

3. Detect input file type in file conversion service


