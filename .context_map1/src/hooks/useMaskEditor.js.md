**Purpose:**

Post processing brush editor hook that lets the user manually paint corrections over the result canvas, returned by an ai service when it completes its processing.

Provides the erase and restore tool.

